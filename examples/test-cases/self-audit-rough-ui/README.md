# Self Audit Rough UI

This test case is intentionally rough. It is used to test whether the audit workflow can find realistic UI issues, then verify that the fix workflow improves the page without changing the product concept.

- `before.html`: intentionally broken page used for the initial audit screenshot.
- `index.html`: fixed version after applying the audit findings.

